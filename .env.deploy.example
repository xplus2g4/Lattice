# Where scripts/deploy.sh deploys. Copy to .env.deploy (ignored) and fill in.
LATTICE_PROJECT=
LATTICE_ZONE=
LATTICE_VM=
# Public clone URL the VM checks out; scripts/deploy.sh --setup passes it to vm-setup.sh.
LATTICE_REPO=
